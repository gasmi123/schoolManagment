String formatDate(String date) {
  // final Jiffy formatter = Jiffy('yyyy-MM-dd HH:mm');
  return date;
  // return Jiffy(date, "yyyy-MM-dd")
  //     .fromNow(); //Jiffy(postModle.postDate, "yyyy-MM-dd").fromNow()
}
